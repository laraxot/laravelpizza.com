Wave aperta: `php artisan ide-helper:models -W` (Issue #569).

Allineamento prima dell'esecuzione:
- docs/rules aggiornate con `ide-helper-models-wave-rule.md`
- docs/memory + docs/skills aggiornate per workflow wave
- docs modulo/tema aggiornate prima delle modifiche runtime
- vincolo operativo confermato: solo fix forward-only con git, mai rollback.

Nel prossimo update porto output comando, root cause aggregate e batch di fix con quality gate finale.
